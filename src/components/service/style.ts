import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flexDirection: "row",
        justifyContent: "space-between",
        borderRadius: 16,
        backgroundColor: "#f5f5f5",
        padding: 16,
        alignItems: "center",
    },
    tag: { fontSize: 12, backgroundColor: "#8A05BE", color: "#f5f5f5", paddingHorizontal: 2, borderRadius: 5 }
})