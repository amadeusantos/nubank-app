import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: { width: 256, height: 200, borderRadius: 16, alignContent: "flex-start", backgroundColor: "#f5f5f5" },
    textTitle: { width: 256, height: 94, borderTopLeftRadius: 16, borderTopRightRadius: 16 },
    textInformation: { marginTop: 8, marginBottom: 4 },
    buttonKnow: { width: 70, fontSize: 12, backgroundColor: "#8A05BE", color: "#f5f5f5", padding: 5, borderRadius: 12 }
})