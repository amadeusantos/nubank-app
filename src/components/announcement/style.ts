import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: { width: 256, backgroundColor: "#F5F5F5", justifyContent: "center", alignItems: "center", padding: 12 },
    textPurple: { color: "#8A05BE" },
    textBlack: { color: "#000" }
})